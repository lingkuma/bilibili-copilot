import type { PromptTemplate } from '../types'

export const defaultTemplates: PromptTemplate[] = [
  {
    id: 'outline',
    name: '总结大纲',
    description: '按时间线生成详细分层大纲。',
    instruction: `请根据下面的 Bilibili 视频字幕生成非常详细的 Markdown 总结大纲。

要求：
1. 用中文回答，内容必须具体、可复查，不能只写泛泛的标题。
2. 必须使用下面的结构：
   # 总结大纲
   ## [MM:SS] 大章节标题
   [<image>@MM:SS]
   ### [MM:SS] 小章节标题
   - 作者观点：详细列出作者在这一小节提出的具体判断、理由、例子或结论。
   - 关键信息：补充这一小节中值得记录的事实、数据、操作步骤、概念解释或上下文。
   - 逻辑关系：说明这一小节与前后内容的承接、转折或因果关系。
3. 大纲必须有清晰层次：用二级标题 ## 表示大的重要章节，用三级标题 ### 表示大章节下的小的关键部分；全文控制在 4-6 个大章节。
4. 每个大章节和每个小章节标题都必须以时间戳开头，例如：## [02:15] 大章节标题、### [03:08] 小章节标题。
5. 每个大章节下面至少包含 2 个小章节；如果视频内容较短，也要尽量按观点变化拆分。
6. 每个小章节都要详细列出作者的具体观点，不要只写一句摘要；优先还原作者的论证、例子、态度和结论。
7. 大章节图片由 AI 决定：每个大章节标题下方必须输出 1 行图片标签，例如：[<image>@02:18]，用于表示该大章节的代表画面。
8. 小章节不要输出图片标签；小章节图片由用户之后手动插入。
9. 图片标签必须独占一行，只能放在大章节标题下方，每个大章节恰好 1 个，全文最多 6 个。
10. 所有时间点必须使用 [MM:SS] 或 [HH:MM:SS] 格式；时间范围可使用 [MM:SS-MM:SS]，但标题开头优先使用单个起始时间点。
11. 不要输出 JSON，不要使用表格。`,
  },
  {
    id: 'brief-outline',
    name: '简略大纲',
    description: '生成较短的核心大纲和小纲。',
    instruction: `请根据下面的 Bilibili 视频字幕生成简略 Markdown 总结大纲。

要求：
1. 用中文回答，重点抓核心结构，不要展开成长篇总结。
2. 必须使用下面的结构：
   # 简略大纲
   ## [MM:SS] 核心大纲标题
   [<image>@MM:SS]
   ### [MM:SS] 小纲标题
   - 用 1-2 句话说明这一小纲的核心内容或作者观点。
3. 全文控制在 3-6 个核心大纲；每个核心大纲下面必须配 2-4 条小纲。
4. 核心大纲和小纲都必须带时间戳，例如：## [02:15] 核心大纲标题、### [03:08] 小纲标题。
5. 每个核心大纲标题下方必须输出 1 行图片标签，例如：[<image>@02:18]，用于表示该核心大纲的代表画面。
6. 小纲不要输出图片标签；小纲图片由用户之后手动插入。
7. 图片标签必须独占一行，只能放在核心大纲标题下方，每个核心大纲恰好 1 个，全文最多 6 个。
8. 所有时间点必须使用 [MM:SS] 或 [HH:MM:SS] 格式；时间范围可使用 [MM:SS-MM:SS]，但标题开头优先使用单个起始时间点。
9. 不要输出 JSON，不要使用表格。`,
  },
  {
    id: 'summary',
    name: '总结视频',
    description: '提炼核心观点、结构和结论。',
    instruction: `请根据下面的 Bilibili 视频字幕生成结构化 Markdown 总结。

要求：
1. 用中文回答。
2. 必须使用下面的结构：
   # 视频总结
   ## 核心结论
   ## 时间线大纲
   ## 关键概念
   ## 可追问问题
3. 时间线大纲必须从头到尾组织成章节，每个章节标题必须以时间戳开头，例如：### [02:15] 标题。
4. 所有时间点必须使用 [MM:SS] 或 [HH:MM:SS] 格式；时间范围可使用 [MM:SS-MM:SS]。
5. 如果某个章节适合展示关键画面，可在该章节标题下方单独输出一行图片标签，例如：[<image>@02:18]。
6. 图片标签必须独占一行，每个章节最多 1 个，全文最多 4 个。
7. 不要输出 JSON，不要使用表格。`,
  },
  {
    id: 'questions',
    name: '生成问题',
    description: '根据字幕创建理解题和讨论题。',
    instruction: `请根据下面的 Bilibili 视频字幕生成 Markdown 格式的问题清单。

要求：
1. 必须使用下面的结构：
   # 视频问题
   ## 理解题
   ## 深入讨论
   ## 对应片段
2. 每个问题尽量带上对应时间点，例如：[03:20] 问题内容。
3. 所有时间点必须使用 [MM:SS] 或 [HH:MM:SS] 格式；时间范围可使用 [MM:SS-MM:SS]。
4. 如果某个问题需要参考画面，可在问题下方单独输出一行图片标签，例如：[<image>@03:20]。
5. 图片标签必须独占一行，全文最多 3 个。
6. 不要输出 JSON，不要使用表格。`,
  },
]

export const findTemplate = (id: string) => {
  const template = defaultTemplates.find(item => item.id === id) ?? defaultTemplates[0]
  if (!template) {
    throw new Error('No prompt templates are configured.')
  }
  return template
}
